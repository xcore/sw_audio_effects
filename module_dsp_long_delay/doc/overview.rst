Overview
========

This application creates long multiple echos.
For a 8 MByte SDRAM, a stereo channel can be delayed by upto 20 seconds.
The input audio stream is fed into a delay-line.
The delay-line returns a set of delayed outputs, creating a multiple echo effect.
